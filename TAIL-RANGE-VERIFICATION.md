# Range 尾帧提前提取验证

本文前半保留早期“整片 + Range 竞速”试验记录。该方案已在本日后续更新中由 Range-only 替代，当前实现与验证见末尾。

日期：2026-09-06；Node 24、FFmpeg 8.1。没有提交任何新视频生成请求，没有清理旧素材，也没有修改模型、提示词、seed 或浏览器播放器。

## 实现范围

- 攻击归档和后续片段共用 `VideoMediaCache.tail()` 的竞速结果，收尾和状态预热仍通过原 continuity promise 接收真实尾帧。
- Range 复用完整下载已收到的文件头，再读取末段关键帧及依赖帧。FFmpeg 负责 MP4 元信息和 B 帧解码，不拿最后一个字节块、近似截图或前一帧冒充尾帧。
- FFmpeg 只读取带随机路径的临时回环 HTTP 服务；外部请求由 Node 发起，复用连接池并限制 fal HTTPS 域名、206 响应、字节范围、文件大小和取消时限。元信息不在头部等情况仍由 FFmpeg seek，遇到不支持或预算超限使用完整下载路径。
- 两路先成功者返回；Range 失败不触发新付费生成或第二次完整下载。完整下载先成功不等待 Range 结束；败方清理及统计只影响归档收尾，不阻塞下一段生成。若完整下载在 Range 胜出后才完成，会跳过备用抽帧所需的临时 MP4 复制、写盘和抽帧；原视频归档继续。
- 暂未把所有场景视频的下载发布流程改为 Range；本次覆盖攻击续段，以及依赖攻击尾帧的收尾和状态素材锚点。

## 实际 CDN 测试

读取既有 `c74cb4a3-...`、`400fa4c1-...` 两个 session 的 clip 0/1，没有新生成。所有竞速输出 JPEG 与本地完整 MP4 使用原提取流程得到的 JPEG 逐字节相同。

最终代码的一组实际对照（耗时为下载开始到尾帧可用）：

| 既有样片 | 原完整下载 | 新竞速 | 实际胜出路径 |
| --- | ---: | ---: | --- |
| 400fa4c1 clip 0，3.82 MB | 6.246 秒 | 1.937 秒 | 完整下载 |
| 400fa4c1 clip 1，2.76 MB | 2.182 秒 | 2.213 秒 | 完整下载 |

**不能把第一行的差值归因于 Range 提速**：该样本仍是完整下载赢，连接热度和 CDN 吞吐不同。第二行略慢也表明不能保证并发 Range 完全没有带宽/CPU开销。本轮实际 CDN 样本尚未证明稳定提速；保留有界竞速作为试运行，并通过后续 `tailSource` / `tailReadyMs` 观察收益。

最初直接远程 FFmpeg 以及初版 Range 测试会重复读取较多探测数据；最终实现改为复用文件头，并合并尾部小请求。旧原型结果不作为最终性能结论。

## 固定限速对照（模拟网络，不是 fal 实测）

使用已有 3,283,084 字节真实视频，本地模拟每请求 150 ms 响应延迟、每流每 100 ms 传输 64 KiB，FFmpeg 真正解码。两次完整下载耗时分别 5.309 / 5.301 秒。

| 路径 | 尾帧就绪 | 完整下载次数 | 额外 Range |
| --- | ---: | ---: | --- |
| 原流程 | 5.454 秒 | 1 | 0 |
| 新竞速 | 1.749 秒 | 1 | 1 次、709,981 字节 |

尾帧完全一致，提前 3.705 秒可供下一段生成。该模型是**每流独立限速**，并非总带宽共享的真实网络；只能证明慢整片下载时链路可以提前，不保证用户网络下相同收益。

## 回归与审查

- `tests/video-tail-range.test.js`：有音轨和 B 帧、moov 位于头/尾的真实解码一致性；共享前缀但整片故意未完成时仍可解码；Range 拒绝、长度/编码/来源校验、超时取消；竞速双向胜出；复用回退；真实 FalVideoRunway 在整片未下载完时提交第二段（提供商为模拟，只有预期两次提交）。
- 原缓存/抽帧用例继续验证播放器渐进接收、读者独立取消、归档失败和容量淘汰保护。
- 独立 `range_review` 子 agent 代码审查 PASS，21 个相关测试通过；另行注入 ETag 变化、响应超长、FFmpeg 不存在、途中取消，均拒绝并清理临时目录。
- 最终全量 `npm test`：306 / 306 通过；包含上一轮尚未提交的失败恢复改动，属于当前工作区整体验证，不是独立干净提交验证。
- 本次没有新的付费端到端战斗，也没有浏览器画面验收；因此不宣称已经解决所有无缝播放问题。
- 确认近期任务均已结束后重启本地服务；首页返回 200，既有场景缓存媒体返回有效 206 / 1024 字节，未刷新用户浏览器或触发预热生成。

复测已有视频（只读取，不生成；远端 CDN URL 过期会失败）：

```sh
node scripts/benchmark-tail-range.mjs .local/runs/<session-id>/clip-0.json .local/runs/<session-id>/clip-1.json
node --test tests/video-tail-range.test.js tests/video-media-cache.test.js tests/video-tail-frame.test.js
npm test
```

协议选项依据：[FFmpeg HTTP 文档](https://ffmpeg.org/ffmpeg-protocols.html#http)。当前安装支持所用 seek/请求尺寸选项；旧 FFmpeg 若不支持会回退完整下载，仍需安装可用 FFmpeg。

## 后续更新：只用共享 Range，不再整片竞速

日期：2026-09-06。应用户要求移除 fal 攻击片的并行全量 GET。`VideoRangeSource` 按 URL 串行下载区间，缓存按偏移共享给 FFmpeg、浏览器和归档。文件头只取一次，末段用于尾帧后保留；全片通过补洞拼齐，不再重传末段。其它场景视频的顺序下载/加工逻辑不在本轮范围内。

浏览器攻击响应带 `rangeOnly`，禁止另开 CDN 整片回退；正常同源缓冲和播放仍保持旧帧，等待有界。Range 不被 CDN 支持时明确失败；局部解码失败可以补齐同一缓存后再解码，不用新的下载源。源错误时释放预分配视频内存、保留小型失败记录，避免错误缓存堆积和重复下载。

### 真实 CDN 单片对照（无生成费用）

已有 `d14f973f-4b05-4fa2-ab88-1a8601e8cc43/clip-1`，原片 2,813,350 字节，顺序执行 full-only、range-only。两次尾帧均与完整本地解码 JPEG 的 SHA-256 相同：

| 路径 | 尾帧可用 | 全片到齐 | 上游传输 |
| --- | ---: | ---: | ---: |
| 完整 GET 对照 | 8.929 秒 | 8.834 秒 | 2,813,350 字节 |
| Range-only | 2.933 秒 | 12.129 秒 | 5 次 Range，共 2,813,350 字节 |

尾帧提前约 6 秒，但整片下载变慢约 3.3 秒。该结果仅为一次真实网络样本，包含连接热度及 CDN 波动，不代表稳定提速比例。确定的改变是无整片并行下载、无成功区间重复传输，以及取得尾帧后可立即推进下一段；不能承诺全片更快或永不等待。

复测脚本 `scripts/benchmark-tail-range.mjs` 现在比较 full-only 和 range-only，不再比较旧竞速模式。仅下载已有视频，URL 过期会失败。

### 内置浏览器双片回放（模拟传输，无生成）

`node scripts/verify-range-only.mjs .local/runs/d14f973f-4b05-4fa2-ab88-1a8601e8cc43`：使用已有 clip 0/1、实际 `VideoMediaCache` / `AttackVideoPlayer` / FFmpeg，模拟每请求 150ms、每 50ms 64KiB，第二片下载在首片尾帧到达后才启动。独立端口运行，不写原缓存，不调用模型。

- 首片真实首帧 691ms；第二片 2834ms 已缓冲；首片结束 6626ms，第二片起播 6642ms，交接 16ms；11873ms 第二片播完。
- 首片 8 个 Range，共 3,685,794 字节；第二片 7 个 Range，共 2,813,350 字节。分别与原片大小一致，区间无交集、同 URL 最大上游并发均为 1。
- 两片完整播完，未记录 `playback_waiting` / `playback_stall_ms`；已检查结束截图，画面保留真实末帧。测试未模拟 fal 生成时间，不能据此宣称真实生成战斗的片间一定只有 16ms。

### 回归及准出

全量 318/318 测试通过（包含工作区原有未提交改动）。独立 `range_only_review` 先发现失败缓存内存释放问题，修复并新增回归后复跑 38 项相关测试全部通过，最终 PASS。覆盖真实 B 帧/音轨、moov 头尾、串行/去重/补洞、首帧与续段、源变更、非法响应、取消、内存、原子归档与播放器禁 CDN 回退。

最近攻击和场景任务已完成后重启了 4173 服务；首页 200，历史攻击片 `bytes=0-1023` 返回 206 和 1024 字节，新客户端包含 Range-only 回退限制。独立回放服务与测试页已关闭。未刷新用户原页面、未清理原缓存、未提交新生成；用户需刷新页面加载客户端改动。

## 后续优化：按缺口补齐（最终保留）

本轮最终只调整归档读取边界，不改画质、尾帧探测、FFmpeg 解码参数、生成请求或代理：

归档从当前实际缺口开始读取，最多 1MiB，每块完成后重新排队。避免固定文件对齐边界把连续缺口拆出短请求，保留尾帧/播放器优先级及零重复字节。

### 撤回的试验：小探测块

曾尝试把文件头后的开头关键帧探测从 1MiB 缩小为 64KiB。真实 CDN 尾帧变快，但同片同模拟网速的浏览器对照暴露了首片缓冲副作用，故该代码和专属测试已完整撤回。下表属于试验记录，**不是最终交付收益**。

输入 `.local/runs/40fbd34d-6a07-4474-baa6-dfdee92ed479/clip-{0,1}`，仅下载必要 Range，未调用付费模型。修改前的探测逻辑仅在独立测试进程内还原，不写回工作区。第一片旧→新，第二片新→旧，依次执行，避免测试互相抢带宽。

| 片段 | 探测方式 | 尾帧读取字节 | 请求数 | 尾帧耗时 |
| --- | --- | ---: | ---: | ---: |
| clip 0 | 修改前 | 1,682,379 | 3 | 4.926s |
| clip 0 | 小探测块 | 699,339 | 3 | 3.615s |
| clip 1 | 小探测块 | 765,558 | 2 | 1.647s |
| clip 1 | 修改前 | 765,558 | 2 | 1.617s |

四次 JPEG 均与本地完整原片解码的 SHA-256 一致。首片减少 983,040 字节（约 58%），此次耗时减少约 27%；第二片本来无开头补探测，字节不变、耗时基本不变。单次网络对照受 CDN 与连接热度影响，不能承诺固定提速比例。完整文件最终仍须下载供播放，减少的是尾帧关键路径字节，并非把整片总流量减少 58%。

修改前浏览器对照无 `playback_waiting`，首片尾帧 2249ms、第二片缓冲 2767ms；小探测试验提前到 1462/1976ms，但首片发生 1079ms 片内缓冲。为优先保持连续播放，没有保留这项尾帧关键路径提速。

### 保留方案的证据与范围

同片的本地无播放干扰归档中，clip 1 请求数从 5 降至 4，总字节均为 2,849,174；该片没有触发小探测，因此结果不依赖被撤回的优化。并发播放会影响缺口顺序，不能承诺所有片都少一次请求。

现有 Node 请求与显式使用已有本地代理的四次 1MiB Range 对照无稳定收益（旧方式 4.810/3.818s，现有代理 3.235/5.014s），因此未更改系统或项目代理。

### 回归和边界

最终全量 320/320 测试通过，包含工作区已有改动。独立 `range_efficiency_review` 确认小探测撤回后复跑相关 17/17 测试，最终 PASS：moov 头尾/B 帧/音轨、尾帧一致性、按洞补齐、队列公平性、异常响应与共享下载均通过。

首帧即播与不可抢占的共享网络请求仍可能产生片内缓冲，局部优化不等于所有网络下都无缝。本轮没有增加起播等待第二片的逻辑，也没有恢复并行完整 GET。

### 最终浏览器同片同网速复测

仍使用上述两片和 `verify-range-only.mjs`（150ms/请求、64KiB/50ms），修改前对照与最终仅补洞版本分别在独立服务顺序回放：

| 项目 | 修改前 | 最终仅补洞 |
| --- | ---: | ---: |
| clip 0 请求数 / 完整到齐 | 8 / 3.863s | 8 / 3.876s |
| clip 1 请求数 / 完整到齐 | 7 / 3.342s | 6 / 3.182s |
| 首片结束 → 次片起播 | 3ms | 3ms |
| 片内 waiting/stall 记录 | 无 | 无 |

最终首片起播 577ms，第二片 2807ms 已缓冲；首片 6659ms 结束，第二片 6662ms 起播、11911ms 结束。两片请求字节分别为 3,303,781 / 2,849,174，等于原片大小，无重叠，上游同 URL 最大并发为 1。保留首片原有缓冲行为，删除了一次第二片补洞请求。模拟仅覆盖传输/解码/播放，不包含真实 fal 生成耗时；3ms 不是线上生成连续性的保证。

最终已确认最近演出和场景任务完成后重启 4173；首页 200，已有片 `bytes=0-1023` 返回 206/1024 字节。独立测试页及三个诊断服务已关闭，原游戏页未刷新，原缓存未清理，付费生成调用为零。
