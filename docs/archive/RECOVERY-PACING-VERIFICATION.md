# 收尾分镜节奏验证

> 历史验收记录：内容对应记录时的版本，不作为当前运行说明；文内代码路径及命令以项目根目录为基准。

日期：2026-09-06。

## 修改范围

- `src/battle-recovery.js`：收尾提示改为 0–1 秒余势、1–2.5 秒反应特写、2.5 秒直接切回同框，4.5–5 秒保持第三镜。保持总长 5 秒，不倍速。
- 删除强制慢退归位，低 HP 通过在位疲态表现；保留两侧异常状态、昏厥卷卷眼和不能移动者的实际位置，切镜期间画外一方也不能变更状态。
- 真实攻击尾帧仍为首帧，不改为参考图重建；并发、播放与战斗结算未变。
- `battle-recovery-v2` 不命中旧慢镜头缓存，不删除旧文件，不改变待机／听令缓存。
- `src/app.js` 与 `src/ui-localization.js` 的收尾提示改为“保留战后状态，回到双方同框”，同步英文／日语。

## 验证结果

- 收尾、播放、场景生成服务、多语言定向测试：52/52 通过。
- `npm test`：274/274 通过，0 失败，结果保存在 `.local/recovery-pacing-tests.txt`。
- 独立子 agent `command_review` 只读复审 PASS，无阻断；独立全量同样 274/274 通过。
- 本地 4173 服务重启后，只读 HTTP 检查确认正在提供 `battle-recovery-v2` 与新三镜代码，未刷新用户战斗页面。

## 成片验证边界

原计划仅复用真实回合 `5101ab37-a7c6-4f7b-8a3e-338770110a9e` 的末攻击片（index 1）生成一段新收尾，不重跑整场。已查看原始尾帧：主要为受击后皮卡丘特写；真实末状态为皮卡丘 53/110 HP、小火龙 44/114 HP，双方无主要异常状态。

付费样片执行在请求发出前被权限审核拦截，本轮没有新的视频生成请求。随后只读检查候选缓存 key `67d6d92cf152a53b891522d64d7116cecb14c61c32f1bda358991953d9724751` 返回 404，磁盘也不存在该素材目录。未通过其他入口重试。

因此本次仅确认提示词、缓存边界、运行服务和回归测试；尚未证明生成模型能按指定秒数切镜或保持画外小火龙造型。尤其原始尾帧只显示一方时，另一方造型恢复仍有既存风险。成片视觉验收待用户明确授权一次外部 fal 付费生成，不将测试通过视为实际成片通过。
