# 状态阻止出招：独立分镜验证

2026-09-05。覆盖麻痹、睡眠、冰冻，以及混乱导致的实际自伤。仅将引擎已经产生的结果加入演出；不重算规则、不额外抽随机数、不为受阻招式消耗 PP。系统语音及此前媒体链路优化未改变。

## 实现与事实边界

- 引擎在状态判定结束后，为 skip / confusion damage 事件补充双方快照及原本拟用的招式。混乱自伤仍扣在原行动者身上，快照记录自伤前 HP，既有事件保留自伤后 HP。
- 每回合记录现在包含真正攻击和状态失败，仍最多两条行动记录。`blockedReason` 为 paralysis / sleep / freeze / confusion；普通麻痹但能动、自然醒来、解冻等仍是正常招式。
- `purpose=blocked` 使用一个 5 秒三镜模板：同场中景→状态角色特写→同场远景。服务端重建提示词，导演不可改写结果或插入攻击。睡眠不睁眼，冰冻不解封，麻痹静电不得成为电系技能。
- 麻痹／睡眠／冰冻必须严格零效果；混乱只允许符合原公式的 `selfDamage` 和相符的 `actorFainted`。只有混乱自伤使用 2.4 秒同步点，其他状态没有命中同步点。视频事件采样沿用现有 timeupdate 阈值，并非逐帧准确计时承诺。
- 行动游标、尾帧状态、后续生成与状态素材预热沿用现有管线。混乱自伤不会在特写开始前提前应用，取消或跳过仍完成且只应用一次事实事件。
- 旧分镜/素材不补生成、不删除。新增加的状态片在正常对战中会真实计费；不会把已有状态标签本身当成一次失败来重复生成。

## 自动测试与独立审查

主线程及独立审查均 `npm test`：169/169 通过。新增测试在 `tests/paralysis-storyboard.test.js` 与 `tests/blocked-state-storyboard.test.js`，共 17 项；两个旧测试按新增睡眠记录调整预期。

覆盖先后手、双方都受阻、新施加睡眠／冰冻后当场受阻、自然醒来／解冻、混乱先于麻痹、混乱自伤和自伤 KO、挣扎被阻止无反伤、招式资格和状态/HP 连续性、伪造伤害拒绝、道具／换人分支、取消一次性结算，以及真实 Turbo 跑道的尾帧与 seed 参数。

独立审查 agent Ampere 额外运行 2,000 组旧新引擎对照：战斗 state 与 RNG 调用次数均无差异；全部记录通过服务端校验。代码审查结论 PASS，未发现阻断项。

## 实际网页回放

隔离工具 `.local/blocked-state-replay.mjs`，运行目录 `.local/blocked-replay-5NYmrn/`。实际网页、实际本地服务与 FFmpeg；fal/CDN 被模拟，播放旧付费视频作为传输替身，未发起新付费生成。

这些回放只证明路由、顺序、UI、状态结算和接播，**不是新生成状态动画的视觉验收**。替身中的角色/动作可能与当前事实不符；睡眠截图中的旧动作不能当作本轮新睡眠动画。四类最终画面仍需要后续真实生成样片检查。

| 场景 | 回放记录（运行目录下 `.local/runs/`） | 结果 |
| --- | --- | --- |
| 睡眠 | `153df34c-5b08-4f80-a863-366d349b02d5/playback.jsonl` | 攻击→独立 blocked；7080ms 前片结束，7098ms 后片开始；无后片伤害同步点。期间显示“睡眠，本次招式未能使出”和熟睡播报，HP 面板隐藏；结束后 TURN02，双方 HP 不变、睡眠保留 |
| 冰冻 | `99bb3414-30c9-45e6-a649-901965401d44/playback.jsonl` | 攻击→独立 blocked；7076ms→7093ms 接播；无后片伤害同步点。显示冰冻受阻，结束后状态保留、操作恢复 |
| 混乱自伤 | `e74b6069-b3b7-4fa3-b5d4-af0802399209/playback.jsonl` | 攻击→独立 blocked；记录 selfDamage=18，第二片有独立自伤同步点；回合结束小火龙 85%、皮卡丘 110/110，操作恢复 |
| 麻痹 | 同目录 plan/playback 日志 | 两片完整播放后 TURN02，麻痹保留，双方 HP 不变 |
| 自伤片中途跳过 | `?case=confusion&qa=cancel` 回放 | 在“混乱自伤，本次招式未能使出”时跳过，立即恢复 TURN02；自伤结算到 85%，未重复扣血，玩家 HP 不变 |

睡眠播放 UI 截图：`.local/blocked-replay-5NYmrn/sleep-player-proof.jpg`。本轮没有进行新生成画面、真实 fal 速度或新音效验收。

修改前生产文件备份：`.local/backups/paralysis-beat-20260905/`。

## 本地交付

最终再次全量测试通过。确认用户原页面在等待指令后重启 4173 服务，未刷新用户页面；首页只读检查 200，新分镜模块包含四类 blockedReason。4193 隔离测试服务及其测试标签页已关闭，全部证据保留。用户刷新后完整加载新前端（刷新会重开当前对战）。
