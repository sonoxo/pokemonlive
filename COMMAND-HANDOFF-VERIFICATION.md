# 听令再检查与当前演出素材保护

日期：2026-09-06。范围仅为前端听令选择、素材生命周期及回归测试；不修改生成提示词、战斗规则、缓存键或已付费文件。

## 行为

- 选招时持有当前画面阵容的临时引用，再进行规则结算。下一状态的取消筛选不会取消当前仍需要的听令／收回任务。
- 复用既有预热任务。在训练家剪影播放期间接收迟到的听令并预载，校验阵容、状态、语言、素材 key、URL 与浏览器解码状态。
- 一旦就绪，锁定听令视频与攻击首帧来源；规划也完成后立即提交攻击，不等剪影或听令播完。不能在后台用一种锚点生成攻击，前台却补播另一种听令。
- 剪影结束时最后检查一次；仍未解码则采用原有兜底来源，不延长听令准备窗口，也不再接受迟到素材抢播。
- 当前听令槽在攻击真实首帧接管前禁止被未来状态素材覆盖。攻击接管、跳过、失败和重开时释放引用，恢复普通过期任务清理。
- 增加 `bridge_ready`、`bridge_unavailable` 事件，保留现有 `bridge_playing` 播放证据。

取舍：已有听令就绪时保持立即提交攻击；未就绪时可能等到现有剪影窗口结束才使用兜底提交。这保证听令与攻击锚点一致，并非承诺减少 fal 推理／排队耗时。

## 自动测试

`npm test`：285 / 285 通过。`node --check`：三个改动的生产 JS 文件通过。

新增 `tests/command-handoff.test.js` 七项覆盖：

1. 剪影期间下载／解码就绪即选定，之后不得换锚点。
2. 截止边界再检查，超时后的迟到结果不抢占。
3. 重复使用、错误语言／阵容／类型／解码与取消。
4. 当前及未来素材并发、多个消费者、幂等释放和取消清理。
5. 真实 `beginCinema` 与预载／绑定函数：换后结算到冰冻也保留当前听令，禁止未来状态偷换播放器。
6. 真实入口的截止兜底与取消。
7. 真实攻击请求：规划先完成时等待共享选择，解码就绪后在剪影结束前提交同一 key；等待期间取消则没有 POST。

## 离线浏览器实测

测试入口 `.local/command-handoff-replay.mjs`，端口 4194，独立运行目录 `.local/command-handoff-replay-TjLfNS`。实际页面、客户端模块、服务端调度、下载、FFmpeg 尾帧、视频解码与播放均运行。所有云端 fetch 被本地模拟器截获，未知外部主机拒绝访问，凭据为测试占位符，语音禁用。复用旧缓存视频验证时序；片内角色画面不代表新阵容生成质量，也不验证真实语音或 fal 速度。

首轮刻意让听令在用户选招后才开始完成，按确定性规则让小火龙进入麻痹；随后换上杰尼龟，再连续使用两次撞击。页面最终返回 TURN 05、等待指令，四个主命令按钮可操作，没有演出失败。

| 路径 | 听令选定 | 剪影结束 | 听令真实首帧 | 攻击 session 建立 | 演出完成 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 首轮，听令迟到／本轮新增麻痹 | 1459 ms | 5010 ms | 5027 ms | 1466 ms | 25984 ms |
| 换入杰尼龟后的第一次攻击 | 2 ms | 5017 ms | 5047 ms | 24 ms | 26107 ms |
| 换入杰尼龟后的第二次攻击 | 2 ms | 5031 ms | 5066 ms | 35 ms | 26154 ms |

时间均从各次选招开始计，来自离线模拟，不是线上生成性能数据。换人后的对手攻击分支也完整结束，该分支按原逻辑不播放我方听令。

第二次出招前，听令元素仍为 `readyState=4`，停在旧片结尾 `currentTime=5.2`；剪影结束后实际 DOM 为 `command-bridge` 可见、`currentTime=0.797`，同一素材 key。截图检查确认舞台直接播放、没有弹窗，之后完整回到待机。听令只生成／预载一次，没有因第二次出招重购素材。

运行日志中没有场景素材 DELETE；首轮旧状态听令没有被新增麻痹的结算结果误取消。各攻击 `playback.jsonl` 与网络事件留在上述独立运行目录。

测试服务和测试页均已关闭。生产 4173 服务及用户原游戏页未重启／刷新；现有页面需刷新才能加载新的前端模块。此次没有付费生成，没有验证充值后的 fal 账户状态。

## 审查

独立子 agent `command_review`：生产改动 PASS，无阻断项；独立全量测试及迟到／取消／已就绪三条真实请求入口检查通过。最终新增七项测试独立重跑 7 / 7 通过，文档复核 PASS，可以准出。按非阻断建议补充“规划也完成”的提交条件，并更正旧默认听令文件的说明。
