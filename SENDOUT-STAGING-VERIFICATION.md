# 换人投球空间约束与动画缓存清理

日期：2026-09-06。

## 修复依据与范围

已对最近的皮卡丘换杰尼龟视频进行抽帧检查：球先覆盖小火龙头部，再从头部附近起飞。这一错误存在于生成的视频中，不是网页叠加坐标错误。原始放出提示只描述“画外训练家向空位抛球”，缺少明确的画外起点、训练家所属侧与身体避让要求。

`src/scene-video-service.js` 现对 `sendout` 与旧 `switch` 入口共用投球约束：

- 我方训练家位于镜头后方左侧画外，球从左画边进入，沿左前景短弧线飞向我方空位。
- 对方训练家位于对方站位后方右侧画外，球从右画边进入，沿右中景短弧线飞向对方空位。
- 投球过程中机位／轴线不翻转；球与放出光效和留场者之间保持可见间隔，不能从头顶或身体出现、覆盖脸部、穿过身体、融合形体，留场者也不能持球、投球或为球让位。
- 放出片开头 0–0.8 秒保持空位且没有球，随后出现一颗连续从画外飞入的球。保持新物种身份与双方已有状态；5 秒片长、模型、参考图、并发与播放流程均未更改。
- 新缓存版本 `sendout-staging-v2` 仅作用于上述两种入口，避免新请求命中旧错误镜头。

## 验证

- 定向测试 45/45 通过。
- 全量测试 278/278 通过，日志 `.local/sendout-staging-tests.txt`。
- 独立子 agent `command_review` 最终复审 PASS，无阻断；独立全量同样 278/278 通过。审查覆盖双侧轨迹、状态约束、版本边界与缓存迁移风险，不代表成片视觉通过。
- 新增双方 × 两种入口 × 三种语言的起点／路径约束测试，另覆盖入场与留场睡眠／冰冻、旧换人缓存隔离，以及待机／听令／收回缓存键不变。
- 历史应答测试中“换人缓存键不变”的断言已改为精确验证仅增加投球版本，而非删除断言。
- 本地 4173 源文件只读检查包含新版与双侧入口，旧错误换人 key `b37f09376f65de31f4b661c5ac6a12bc7ecdf7493d2f9e361b2782cac25ea60f` 查询返回 404，固定训练家收回剪影 HEAD 返回 200。
- 本轮未调用付费视频生成，因此未完成新样片视觉验收。提示词改善不是生成模型绝不会出错的保证；Reference 尾帧仍不是硬首帧约束。

## 已执行的缓存清理

服务无监听、页面停在 TURN 01 等待指令时，将两个活动目录整体移出并重建空目录，没有永久删除付费素材或凭证：

| 活动目录 | 原任务目录数 | 文件数 | 字节数 | 清理后条目数 |
|---|---:|---:|---:|---:|
| `.local/runs` | 107 | 756 | 552292279 | 0 |
| `.local/scene-videos` | 156 | 645 | 464522162 | 0 |

恢复备份位置：`.local/backups/animation-cache-reset-gesC5Eqx/`。迁移前后逐文件内容 SHA256 汇总一致：

- runs：`490257da7268cdd4b8c16b8cce6ef421fad16ad1b4c739d9fb271eb956ba14e8`
- scene-videos：`049198e139086e5d6266d19b4e8d085571422e803acd495848d729f21beeaa27`

合计移出 1016814441 字节，约 970 MiB；文件留在备份，因此不释放这些文件占用的磁盘空间。保留项目固定动画、默认待机、图片、语音缓存、历史离线测试夹具及其它备份。生产服务只读取新空活动目录，不会自动扫描恢复备份。

服务已重启以清除内存任务与下载缓存。用户当前网页未自动刷新，避免触发三段初始付费预热；使用新版前必须刷新网页（仅点“重新对战”不足以清除页面素材 Map）。刷新将重置当前战斗并按现有机制重新生成缺失的听令／双方收回；后续行动也会重新产生相关视频费用。备份只用于人工核对或恢复，不代表本次主动清缓存后仍能自动免除重复生成费用。
