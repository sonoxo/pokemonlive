# 固定训练家剪影片与提前预热

2026-09-06，`/Users/superx/pokemonlive`。

## 最终素材

按用户最终要求：直接文生视频，不使用参考图，不指定小智，采用匿名训练家剪影和宝可梦电视动画式的二维赛璐璐表现。

| 用途 | 本地文件 | 实测规格 |
| --- | --- | --- |
| 喊招、抬手指挥 | `assets/video/trainer-command-v1.mp4` | H264，1344×768，5.000 秒，无音轨 |
| 换人、举起闭合精灵球 | `assets/video/trainer-recall-v1.mp4` | H264，1344×768，5.000 秒，无音轨 |

两段通过 [fal H3 Max Turbo 文生视频接口](https://fal.ai/models/minimax/h3-max-turbo/text-to-video/api) 各提交一次，原生请求 duration=5、resolution=768P、aspect_ratio=16:9、seed=42、balanced，安全检查开启。模型实际输出尺寸如上；没有把请求比例当作精确文件尺寸。以 FFmpeg 截取前 5 秒、移除模型音轨并设置 faststart，原片保留在 `.local/trainer-assets/`。

生成脚本：`scripts/generate-trainer-videos.mjs command|recall`。提示词和请求台账：`.local/trainer-assets/command-v1.json`、`recall-v1.json`。请求前以 wx 写入台账，提交不明时阻止再购；已保存请求只恢复查询／下载。首次新生成会计费，运行时不再购买这两个片段。

提示词核心：原创匿名戴帽训练家、逆光深色剪影、手绘线条、两级赛璐璐阴影、晴天松林；喊招片从胸前预备到单次向前指挥，举球片从腰部抬起同一颗闭合精灵球；不生成实际召回、放出、其他角色、字幕或台词。完整最终英文提示词在脚本 shared/action 段及台账 input.prompt，便于复现。

此前临时矢量方案和未成功的小智首帧请求均不是本次交付。最终没有使用图片生成首帧，两个视频都是文生视频产物。

## 播放与并发

- 喊招：点击即开始规划、语音请求和视频准备；本地剪影真实首帧解码后才发声。5 秒剪影结束后接已缓存的宝可梦特写，再接战斗。若台词仍未结束，可在剪影尾帧多停留最多 1.5 秒；静音或失败不阻断演出。不存在等待第二个战斗片起播的门槛。
- 换人：我方举球 → 收回 → 放出 → 新阵容待机／后续对手行动。匹配收回 ready 即提交放出，放出 playable 即预热新阵容待机、听令和两侧收回。服务器仍核验源阵容并等待真实尾帧；不是猜帧并发。训练家视频不作为任何宝可梦生成输入。
- 当前侧的待播收回播放器在新阵容预热开始前被保留；直到放出真实首帧出现才释放。另一侧预载不受影响。
- 本地剪影片预缓冲／首帧／卡顿有 2 秒保护；失败退回原有演出，不重新生成。保持旧画面直到新帧到达；跳过／重开取消视频和语音门禁，旧回调不能重播。
- 特写是否可用仍沿用点击时的场景匹配与 readyState 检查；未缓存特写继续原来的安全回退，不承诺所有阵容都已预制完毕。

## 验证证据

1. 两段素材各抽取 10 帧，查看 `.local/trainer-assets/command-review.jpg`、`recall-review.jpg`：动作顺序和剪影／绘制风格可辨认，没有 3D、像素、UI 或烧录台词；举球片中球始终握在手中且未打开。主线程和独立审查 agent 都查看过。ffprobe 独立确认实际规格与无音轨。
2. `npm test`：203/203，0 失败。日志 `.local/trainer-video-tests.txt`。新增测试覆盖真实首帧与语音门禁、视频失败、跳过与迟到回调、结束单次通知、提前放出／待机预热、阵营区分及收回槽保留。
3. 实际浏览器：4193 隔离回放，训练家片为本次真实新素材，其余生成使用历史视频与假云端，避免额外付费。完整喊招回合 run `84710766-a6a4-4f45-b845-995ed3fdb306`：剪影首帧 56 ms，语音播放 73 ms；首两片分别在 668/1885 ms 缓冲；剪影结束 5032 ms，宝可梦特写 5046 ms，战斗首帧 10296 ms，完整结束 20781 ms。证据 `.local/classroom-speed-replay-wPEqR5/.local/runs/.../playback.jsonl`。这是受控回放，不是真实 fal 速度保证。
4. 换人浏览器：选择杰尼龟，1.84 秒截图仍显示举球片，此时旧皮卡丘收回地址保持正确、放出 video.readyState=4；完整结束 TURN 02，杰尼龟 107/119、小火龙 100%，符合固定 RNG 的对手火花结算，无控制台错误。后续媒体是测试替身，本项验收编排、来源和状态，不声称重新生成了换人质量样片。
5. 静音／跳过／重开：剪影中静音按钮 aria-pressed=false；跳过再重开后所有演出 video 不可见、TURN 01、皮卡丘 110/110。下一回合静音下正常切入宝可梦特写（截图 currentTime=1.396），血量面板 hidden，旧剪影没有重新接管。

## 审查门禁

独立审查曾判 FAIL：提前预热可能把新阵容收回片写入旧阵容尚未播放的同一播放器。已加入 run.recallVideo 保留及真实函数复现回归，直到放出首帧才释放。复审 PASS；独立全量 203/203、专项 6/6 通过，未发现其他阻断。主线程随后完成上述浏览器验收。

最终范围：新增两个固定付费素材；网页像素 UI、宝可梦战斗风格、规则与真实尾帧链不变。举球／喊招过渡提供生成时间窗口，但不能保证外部排队或下载总在窗口内完成。
